    <style type="text/css">
      html { height: 100% }
      body { height: 100%; margin: 0; padding: 0 }
      #map_canvas { height: 100%; border-color: 1px solid #000000 }
    </style>

<script type="text/x-handlebars">

    <div class="button_block">
    {{#view App.mapTracksView}}
    	{{view Em.TextField placeholder="Asset" valueBinding="App.mapTracksView.asset"}}
        <div class="button_tracks">
          <button class="btn" {{action "loadTracks" target="App.tracksController"}}>
              {{view.name}}
          </button>
        </div>
    {{/view}}
    </div>

</script>

<div id="map_canvas" style="width:100%; height:700px;"></div>
